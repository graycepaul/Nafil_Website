# URL of the Nafil Mobile web app (deployed separately) — the "Get started"
# link points here. Copy this file to .env.local and point it at whatever
# you're testing against locally, e.g. the Mobile app's own dev server:
NEXT_PUBLIC_APP_URL=http://localhost:8081
